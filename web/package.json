{
  "name": "valheim-cheat-radar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "wasm:build": "node scripts/build-wasm.mjs",
    "build:single": "npm run build && node scripts/build-single-file.mjs"
  },
  "dependencies": {
    "fzstd": "^0.1.1",
    "leaflet": "^1.9.4",
    "preact": "^10.26.9"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/leaflet": "^1.9.22",
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.3"
  }
}
